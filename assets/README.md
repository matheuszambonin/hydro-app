# assets/

Coloque aqui `app_icon.ico` (ícone do instalador e do executável).

Se este arquivo não existir, tanto o `app.spec` quanto o `installer.iss`
foram ajustados para não falhar — apenas usam o ícone padrão do
PyInstaller/Inno Setup. Adicione o `.ico` assim que tiver a arte final.
